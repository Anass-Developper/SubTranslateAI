{
  "name": "dual-subtitles",
  "version": "1.0.0",
  "private": true,
  "description": "Double sous-titrage français–chinois simplifié dans Chrome et Edge.",
  "type": "module",
  "workspaces": [
    "apps/extension",
    "apps/local-server",
    "apps/desktop",
    "packages/shared"
  ],
  "engines": {
    "node": ">=20.18.0",
    "npm": ">=10.8.0"
  },
  "scripts": {
    "dev": "npm run build:shared && concurrently --kill-others-on-fail --names serveur,extension --prefix-colors cyan,magenta \"npm run dev:server\" \"npm run dev:extension:watch\"",
    "dev:server": "npm run dev --workspace @dual-subtitles/local-server",
    "dev:extension": "npm run build:shared && npm run dev:extension:watch",
    "dev:extension:watch": "npm run dev --workspace @dual-subtitles/extension",
    "build": "npm run build:shared && npm run build --workspace @dual-subtitles/local-server && npm run build --workspace @dual-subtitles/extension && npm run build --workspace @dual-subtitles/desktop",
    "build:shared": "npm run build --workspace @dual-subtitles/shared",
    "build:server": "npm run build:shared && npm run build --workspace @dual-subtitles/local-server",
    "build:extension": "npm run build:shared && npm run build --workspace @dual-subtitles/extension",
    "build:desktop": "npm run build:shared && npm run build --workspace @dual-subtitles/extension && npm run build --workspace @dual-subtitles/desktop",
    "package:windows": "npm run build:server && npm run build:extension && npm run package:windows --workspace @dual-subtitles/desktop",
    "test": "npm run build:shared && npm run test --workspaces --if-present",
    "lint": "eslint . --max-warnings 0",
    "lint:fix": "eslint . --fix",
    "typecheck": "npm run build:shared && npm run typecheck --workspaces --if-present",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check": "npm run format:check && npm run lint && npm run test && npm run build"
  },
  "devDependencies": {
    "@eslint/js": "^9.22.0",
    "concurrently": "^9.1.2",
    "eslint": "^9.22.0",
    "globals": "^16.0.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2",
    "typescript-eslint": "^8.26.0"
  },
  "license": "SEE LICENSE IN LICENSE"
}
